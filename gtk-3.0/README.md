# gtk-3.0

legacy gnome baggage that refuses to die peacefully.

why does css have to exist everywhere

- `settings.ini`: tells gtk3 to stop using adwaita light and burn your retinas.
- `gtk.css`: imports colors.
- `colors.css`: matugen's victim. dynamically extracted material palette.
- `bookmarks`: quick file picker paths so you don't navigate `/` like an archaeologist.
