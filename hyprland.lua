-- the bare essentials so the system doesn't explode immediately the second i log in
-- no drama. just requires.

require("config")
require("colors")      -- matugen colors (auto-generated)
require("monitors")
require("anims")
require("binds")
require("startup")
require("rules")
require("gestures")    -- optional, delete if you don't use touchpad

-- HyprMod managed settings
require("hyprland-gui")
