# yazi

blazing fast terminal file manager written in rust because navigating directories in bash is a crime against humanity.

arrow keys and vim motions go zooom

- `theme.toml`: synched with matugen.
- `keymap.toml`: custom keybindings.
- `package.toml`: plugin manifests.
- `plugins/`: custom yazi extensions like smart-enter.
