# fastfetch

the neofetch killer because we need system flex stats in 0.002 milliseconds or our attention spans collapse.

look at my ram usage and tremble

- `config.jsonc`: json with comments because fastfetch actually has taste. displays distro, host, kernel, uptime, packages, shell, wm, memory, and color chips.

matugen dumps fresh colors into this every time you change wallpapers.
